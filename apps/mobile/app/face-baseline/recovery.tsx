// DORMANT route: fully built but unreachable until the native face-lost event wires FB_ROUTES.recovery.
// Not an orphan; do not delete. See docs/healthcheck/REPORT.md P2-2.
/** expo-router route → FaceDetectionRecoveryScreen. Implementation lives in features/face-baseline. */
export { default } from '../../features/face-baseline/screens/FaceDetectionRecoveryScreen';
