/**
 * @module features/devices/screens/routes
 * @description Route constants for the Devices surface.
 *
 * expo-router resolves routes from the filenames under `app/`, so a typo in a
 * pushed path fails silently at runtime. Naming the route once here lets a
 * test assert the screen file behind it actually exists.
 */

/** The Devices connection screen, reached from Lab → Devices. */
export const DEVICES_ROUTE = '/devices';
